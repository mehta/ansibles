---
# file: nginx/tasks/sites.yml


## For DEBIAN nodes
- name: sites | DEBIAN | update configurations for sites inventory
  file:
    src={{ item.path }}
    dest="{{ nginx_dir }}/sites-available/{{ item.name }}"
  with_items: nginx_sites
  when: item.name is defined and item.name and item.path id defined and item.path and ansible_os_family == 'Debian'