# file: nginx/tasks/modules/authorized_ips.yml

- name: _authorized_ips | DEBIAN | Updated the authorized_ip file
  template:
    src: ../../templates/modules/authorized_ips.j2
    dest: "{{ nginx_dir }}/authorized_ips"
    owner: "{{ nginx_user }}"
    group: "{{ nginx_group }}"
    mode: 0644
  when: ansible_os_family == 'Debian'
