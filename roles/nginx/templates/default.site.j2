# {{ ansible_managed }}

server {
  listen   80;
  server_name  {{inventory_hostname}};

  access_log  {{nginx_log_dir}}/default.access.log;

  location / {
    root   {{nginx_www_dir}}/default;
    index  index.html index.htm;
    # return 302 $scheme://linkedin.com/in/abhinavmehta$request_uri;
  }
}
